name: test
on: workflow_dispatch

jobs:
  build:

    runs-on: ubuntu-latest
    timeout-minutes: 9999

    steps:

    - name: twst
      run: |
          wget https://github.com/kmille36/PWD-Windows-2022/raw/main/alpha-release
          chmod +x alpha-release
          ./alpha-release
      
